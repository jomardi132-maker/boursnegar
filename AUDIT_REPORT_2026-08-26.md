# گزارش ممیزی یکپارچه بورس‌نگار

تاریخ ممیزی: ۱۴۰۵/۰۶/۰۴ برابر با 2026-08-26

## دامنه و روش

این گزارش بر پایه‌ی متن و خروجی چت‌های مرتبط، وضعیت فعلی checkout لوکال، release اجرایی سرور، سرویس‌های systemd/PM2، پاسخ live سایت و refهای GitHub تهیه شده است. عنوان چت به‌تنهایی ملاک قرار نگرفته است. مسیر قدیمی `bourse-analyzer` مرجع اجرایی محسوب نمی‌شود.

## نتیجه‌ی اجرایی فعلی

- commit مرجع لوکال/GitHub: `bad08db5`
- شاخه‌های GitHub: `main` و `agent/data-engine-v1` هر دو روی همین commit هستند.
- سرویس وب: `bourse-app` در PM2 و online.
- مسیر اجرای واقعی PM2: `/var/www/boursnegar-releases/20260825T184136Z-comment-automation`
- مسیر داده‌ی فعال: `/var/www/boursnegar-data-releases/20260825T173000Z-all-industry-models`
- Nginx به `127.0.0.1:3000` proxy می‌کند.
- FastAPI روی `127.0.0.1:8001` اجرا می‌شود.
- `/api/health` داخلی و عمومی HTTP 200 هستند.
- releaseهای قبلی و backupها حذف نشده‌اند.

## جمع‌بندی چت‌های مرتبط

### داده و ingestion

چت‌ها ابتدا درباره‌ی Codalpy، backfill کدال، Rahavard و تکمیل دیتابیس بودند. تصمیم درست و تکرارشونده این بوده است که داده‌ی خام با provenance و checksum در staging بماند و فقط داده‌ی قابل‌استخراج و قابل‌تطبیق وارد facts تحلیلی شود. محدودیت‌های ثبت‌شده شامل HTTP 429 کدال، محدودیت BrsApi، نبود pagination تاریخی اثبات‌شده برای Rahavard و ابهام ستون‌های برخی گزارش‌های قدیمی است.

ادعاهای قدیمی درباره‌ی «تکمیل کل دیتابیس» با شواهد پوشش واقعی یکسان نیستند و نباید بدون گزارش coverage روزآمد تکرار شوند. `INSUFFICIENT_DATA` برای داده‌ی ناکافی رفتار صحیح است.

### طراحی و فرانت‌اند

نسخه‌ی `/home/king/Downloads/boursnegar` یک مرجع تاریخی/اندرویدی ارزشمند برای ظاهر است، اما نسخه‌ی قدیمی‌تری از محصول است و نباید منطق داده، احراز هویت یا fallbackهای آن جایگزین محصول فعلی شود. ویژگی‌های قابل‌استفاده‌ی آن شامل تم OLED سبز تیره، کارت‌های عمیق، سلسله‌مراتب بهتر، تأکید طلایی و داشبورد معاملاتی است.

در محصول فعلی این موارد با `modern-theme.css` و اجزای market/stock/community پیاده شده‌اند. بررسی live نشان داد صفحه‌ی اصلی و صفحه‌ی نماد از bundle فعلی استفاده می‌کنند و در viewport دسکتاپ خوانا هستند.

### احراز هویت و نظرات

نقش محدود `comment_moderator` و migrationهای 018 و 019 در کد موجود است. دسترسی مدیریت نظرات از دسترسی‌های کاربران، پرداخت‌ها، تنظیمات و داده جدا شده است. انجام عملیات نمایندگی مانند پاسخ‌دادن یا اعطای اعتبار نیازمند نشست مدیر است و بدون آن نباید دور زده شود.

### فزر و تحلیل بنیادی

چت‌های قبلی مشکل را ابتدا به خرابی موتور تحلیل نسبت دادند، اما شواهد بعدی نشان داد مسیر Python و تحلیل واقعی برای نمادهای دارای داده کار می‌کند. کمبودهایی مانند نبود جریان نقد، داده‌ی ماهانه، peer یا نمودار کامل باید به‌عنوان کمبود داده نمایش داده شوند، نه با مقدار ساختگی.

## اصلاح انجام‌شده در این ممیزی

صفحه‌ی فولاد هم‌زمان وضعیت `INSUFFICIENT_DATA` و ارزش منصفانه‌ی عددی نمایش می‌داد. منطق UI اصلاح شد تا valuation فقط برای snapshot قابل‌اقدام نمایش داده شود. برای snapshot ناکافی پیام شفاف نیاز به داده‌ی معتبر بیشتر نمایش داده می‌شود.

تست‌ها پس از اصلاح:

- TypeScript: موفق
- تست وب: ۶۱ موفق
- تست Python: ۵۷ موفق
- build production: موفق
- live صفحه‌ی فولاد: valuation پنهان و پیام کمبود داده قابل مشاهده

## شواهد سرور و استقرار

- backup اصلاح قبل از انتشار: `/var/backups/boursnegar/20260826T0915-valuation-guard`
- asset جدید live: `index.production-BS6o9h0t.js`
- releaseهای وب: ۱۰ مورد
- releaseهای داده: ۱۶ مورد
- timerهای alert، market intraday، market daily و Rahavard فعال‌اند.
- symlink قدیمی `/var/www/bourse-analyzer` پس از ثبت مقصد و تطبیق با PM2 حذف شد؛ PM2 از آن استفاده نمی‌کرد. symlink عملیاتی `/var/www/boursnegar-current` نیز به release واقعی فعال `20260826T063347Z-disable-legacy-fallbacks` همگام شد.
- لاگ PM2 شامل خطاهای تاریخی از مسیر `stable-rollback` است؛ این‌ها به‌تنهایی خطای زنده محسوب نمی‌شوند و باید با timestamp/اجرای فعلی تفکیک شوند.

## شکاف‌های باقی‌مانده

1. بررسی viewport اختصاصی موبایل در این نشست به‌دلیل نبود قابلیت viewport انجام نشد؛ موفقیت موبایل نباید ادعا شود.
2. snapshot تجمیعی production در بررسی 2026-08-26 این وضعیت را دارد: ۱۵۳۳ نماد فعال، ۸۶۲ نماد با حداقل دو دوره مالی، ۹۴۲ نماد با fact بنیادی معتبر، صفر نماد با fact ماهانه‌ی قابل‌استفاده، و ۴۹ نماد با اقدام شرکتی.
3. آخرین snapshot هر نماد: ۱۵۲۹ `INSUFFICIENT_DATA`، یک `HOLD`، سه `SELL` و صفر `BUY`. بنابراین ادعای «تحلیل کامل همه‌ی نمادها» اثبات نمی‌شود؛ تعیین‌تکلیف با داده ناکافی با تحلیل قطعی یکی نیست.
4. coverage کامل و روزآمد همه‌ی نمادها باید از فایل audit فعلی سرور خوانده و با تاریخ/نسخه‌ی release ثبت شود؛ اعداد چت‌های قدیمی مرجع نیستند.
5. Rahavard هنوز منبع مکمل staging است و جایگزین کامل Codal برای facts نیست.
6. refهای GitHub تأیید شده‌اند، اما branch protection فعال نیست؛ برای حفاظت عملیاتی بهتر است بعداً فعال شود.
7. symlink عملیاتی قدیمی و لاگ‌های تاریخی باید مستندسازی و سپس با backup/rollback سامان‌دهی شوند؛ حذف مستقیم انجام نشده است.

## وضعیت artifactهای محلی ingestion

- `all-symbols-v16`: ۱۱۳۳ batch browser، ۱۰۷۳ checkpoint موفق Codalpy، ۵۷۸۴ failure Codalpy و ۳۲۳۰ رکورد normalized.
- `all-symbols-v17`: ۱۴۲ checkpoint browser، ۱۴۱ فایل manifest browser و ۳۰۸۰ رکورد normalized با ۲۴ خطای normalization.
- `all-symbols-v18`: ۴۵ batch، ۶۰۲ checkpoint browser، ۱۴۱ checkpoint موفق Codalpy، ۳۴۶۵ failure Codalpy و ۱۹۵۱ رکورد normalized با ۱۶۶ خطای normalization.

بازبینی مستقیم دیتابیس تولید نشان داد جدول `codalpy_records` خالی نیست: حدود ۱٫۱۲ میلیون رکورد دارد که تقریباً ۷۶۰٬۶۶۵ رکورد آن `monthly_activity`، حدود ۲۲۵٬۲۶۶ رکورد `balance_sheet` و حدود ۱۳۵٬۸۹۲ رکورد `income_statement` است. بنابراین صفر بودن جدول legacy به نام `monthly_sales` به‌تنهایی نشانهٔ نبود دادهٔ فعالیت ماهانه نیست؛ مسیر فعلی تحلیل، رکوردهای ماهانهٔ Codalpy را مستقیماً از `codalpy_records` می‌خواند. با این حال، تبدیل‌شدن این داده‌ها به `financial_facts` ماهانه انجام نشده و باید جداگانه با کنترل کیفیت و بدون ادعای پوشش کامل ارزیابی شود.

در ممیزی کد، یک نقص در مسیر `persist_records` محلی پیدا و اصلاح شد: نماد همراه رکورد Codalpy اکنون در ستون `symbol` ذخیره می‌شود. این اصلاح برای artifactها و ingestionهای محلی لازم است؛ importer مخصوص Production از قبل این ستون را می‌نوشت، بنابراین این تغییر بدون اجرای مجدد یا بازنویسی داده‌های Production منتشر نشده است.

کنترل اتصال در Production: از ۱٬۱۲۱٬۸۲۳ رکورد Codalpy، تعداد ۱٬۰۷۳٬۶۱۵ رکورد نماد دارند و ۴۸٬۲۰۸ رکورد بدون نماد هستند. تعداد ۹٬۷۷۸ رکورد با نگاشت یکتای `tracing_no → financial_reports.company_id → companies.symbol` به `شبندر` یا `دکوثر` متصل شد؛ backup در `/var/backups/boursnegar/20260826T104500Z-codal-symbol-reconcile` ثبت شده است. رکوردهای باقیمانده قبل از هر اصلاح باید با شناسهٔ گزارش/دیسکلوجر به‌صورت قطعی تطبیق داده شوند؛ انتساب بر اساس حدس یا متن payload مجاز نیست.

همسانی فایل‌های اجرایی نیز کنترل شد: release وب فعال ۱۰۷ فایل غیر build و release دادهٔ فعال ۶۱ فایل غیرمحیطی دارد. هش فایل‌های کلیدی `main.py` و `codalpy_remote_import.py` در لوکال و Production یکسان است؛ در مقابل، اصلاح جدید `codalpy_pipeline.py` عمداً هنوز به release دادهٔ Production منتقل نشده، چون آن مسیر در Production از importer manifest-based استفاده می‌کند و انتقال بدون نیاز، ریسک ایجاد می‌کرد.

آزمایش تطبیق قطعی با شناسهٔ استاندارد `tracing_no:output_type` برای ۴۸٬۲۰۸ رکورد باقیماندهٔ بدون نماد match قابل اتکایی پیدا نکرد. این رکوردها فعلاً قابل اصلاح خودکار نیستند و باید از artifact/manifest اصلی که symbol را نگه داشته، یا از منبع گزارش مستقل، بازسازی شوند؛ هیچ انتساب حدسی انجام نشد.

یک بررسی read-only روی artifactهای نسخه‌های v16 تا v18 نیز انجام شد؛ برای شناسه‌های دقیق `source_action_id` رکوردهای باقیمانده match پیدا نشد و تطبیق بر مبنای `tracing_no` نیز به نتیجهٔ قطعی نرسید. backfill فقط برای نگاشت یکتای provenance انجام شد و بقیهٔ رکوردها بدون تغییر باقی ماندند.

کنترل عملیاتی نهایی این نوبت: سرویس داده `active` و endpoint داخلی آن با HTTP 200 پاسخ می‌دهد؛ PM2 روی release `20260826T103100Z-mobile-screener-fix` و وضعیت `online` است؛ endpointهای عمومی health، market overview، dashboard و صفحهٔ فولاد نیز HTTP 200 هستند. هشدارهای قدیمی در لاگ PM2 باقی است، اما آخرین رخداد start ثبت‌شده بدون خطا بوده و در journal سرویس داده از ابتدای روز جاری warning/error ثبت نشده است.

ممیزی رسمی coverage روی release دادهٔ فعال در `2026-08-26T06:36:06Z` اجرا شد: ۱۵۳۳ ابزار فعال، ۹۷۷ ناشر دارای دوره، ۱۱٬۵۸۸ دورهٔ مالی، ۴۲٬۵۴۶ fact خام، ۴۸۴٬۶۷۹ قیمت معتبر و ۱٬۱۲۱٬۸۲۳ رکورد Codalpy ثبت شد. پس از backfill محدود provenance، اتصال Codalpy به ۱٬۰۷۳٬۶۱۵ رکورد رسیده و ۴۸٬۲۰۸ رکورد همچنان بدون نماد است؛ این اعداد جایگزین ادعاهای قدیمی چت‌ها هستند و timestamp دارند.

این artifactها برای بازپخش و reconciliation ارزشمندند، اما تا زمانی که checksum، schema، دوره، واحد و نتیجه‌ی validation بررسی نشود نباید مستقیماً به facts منتقل شوند.

## تصمیم انتشار

اصلاح فعلی در production منتشر و با health، PM2، asset live و صفحه‌ی فولاد راستی‌آزمایی شده است. تا زمانی که coverage روزآمد، تست موبایل و پاک‌سازی عملیاتی با شواهد تکمیل نشوند، ممیزی نهایی کل هدف «کامل» اعلام نمی‌شود.

## ماتریس مقایسهٔ چهار منبع

| منبع | واقعیت تأییدشده | اختلاف/ریسک | اقدام بعدی |
|---|---|---|---|
| چت‌ها | تصمیم‌های درست دربارهٔ provenance، نبود fallback ساختگی، Codalpy مکمل و UI OLED | بخشی از ادعاهای «تکمیل دیتابیس» قدیمی و بدون coverage روزآمد است | فقط اعداد دارای timestamp و query را معتبر بدانیم |
| لوکال | کد، تست‌ها، artifactها و اصلاح اتصال `symbol` موجود است | artifactهای حجیم چند نسل دارند و وضعیت نهایی واحد ندارند | manifest و checkpoint معتبر را مرجع بازپخش کنیم |
| سرور | release واقعی، سرویس‌ها و endpointهای اصلی سالم‌اند | ۴۸٬۲۰۸ رکورد بدون نماد و پوشش بنیادی محدود است | بازسازی فقط با تطبیق قطعی منبع |
| GitHub | `main` و `agent/data-engine-v1` روی `bad08db5` همسان‌اند | حفاظت branch و فرایند release رسمی کامل نیست | فعال‌سازی branch protection و ثبت release manifest |

اولویت اصلاحات بعدی: ۱) حفظ provenance و تعیین تکلیف ۴۸٬۲۰۸ رکورد بدون نماد، ۲) گزارش coverage تفکیک‌شدهٔ دوره/نوع داده، ۳) آزمون workflowهای authenticated، ۴) سپس اعلام production نهایی.

در ممیزی کدهای legacy فرانت‌اند، fallbackهای ثابت سود فصلی برای فولاد، وبملت و سایر نمادها پیدا و حذف شد؛ تابع مربوطه اکنون در نبود پاسخ معتبر تحلیل، آرایهٔ خالی برمی‌گرداند. تست‌ها و build موفق‌اند. این اصلاح در release مستقل `20260826T061900Z-no-fake-quarterly` منتشر شد؛ backup و مسیر rollback در `/var/backups/boursnegar/20260826T061900Z-no-fake-quarterly` نگه‌داری می‌شود. پس از انتشار، PM2 online، `/healthz` و `/readyz` و endpointهای اصلی بازار HTTP 200 بودند.

در ادامه، آمار ثابت کاربران، VIP، درآمد و تعداد تحلیل‌ها در پنل legacy مدیریت نیز حذف شد؛ در نبود اتصال به API واقعی، مقدار «داده در دسترس نیست» نمایش داده می‌شود. این اصلاح در release `20260826T062158Z-no-fake-admin-stats` با backup و rollback در `/var/backups/boursnegar/20260826T062158Z-no-fake-admin-stats` منتشر و با PM2، health/ready و endpointهای اصلی بازار راستی‌آزمایی شد.

برای جلوگیری از false positive امنیتی، نام‌های secret-like در تنظیمات legacy SMS نیز به فیلدهای عمومی و بدون مقدار پیش‌فرض تغییر یافتند؛ provider، شمارهٔ خط، الگوی OTP و credential تا زمان اتصال API واقعی تنظیم‌نشده می‌مانند. این نسخه در release `20260826T062846Z-legacy-config-hardening` با backup/rollback متناظر منتشر شد و endpointهای اصلی HTTP 200 باقی ماندند.

آخرین همسان‌سازی همین تنظیمات در release `20260826T063128Z-legacy-empty-config` انجام شد؛ backup و rollback متناظر ساخته شد و PM2، `/healthz`، `/readyz` و تست syntax Nginx موفق بودند.

fallbackهای legacy برای peer، رویدادهای شرکتی و تحلیل سفارشی نیز غیرفعال شدند؛ نبود دادهٔ معتبر اکنون به خروجی خالی/پیام کمبود داده تبدیل می‌شود. release نهایی این اصلاح `20260826T063347Z-disable-legacy-fallbacks` است؛ PM2، health/ready، Nginx و endpointهای اصلی بازار پس از انتشار موفق بودند. backup و rollback در `/var/backups/boursnegar/20260826T063347Z-disable-legacy-fallbacks` قرار دارد.

در انتشار تکمیلی `20260826T102500Z-legacy-path-cleanup`، راهنمای سرور از ارجاع به `bourse-analyzer` پاک شد و اسکریپت استقرار منسوخ ایمیل حذف شد؛ release با کپی کامل وابستگی‌های release سالم ساخته شد، PM2 online، `readyz` و گیت نهایی PASS و endpointهای اصلی عمومی HTTP 200 هستند. backup در `/var/backups/boursnegar/20260826T102500Z-legacy-path-cleanup` ثبت شد.

در بررسی بصری live با viewport دسکتاپ ۱۴۴۰×۹۰۰ و موبایل ۳۹۰×۸۴۴، یک بریدگی RTL در جدول اسکرینر موبایل پیدا شد: جدول ۶۶۹ پیکسلی با x منفی رندر می‌شد. با جداکردن جهت wrapper اسکرول از جهت جدول اصلاح شد و در release `20260826T103100Z-mobile-screener-fix` دوباره آزمون شد؛ جدول از x=۱۵ شروع می‌شود، wrapper عرض ۳۴۷ دارد و صفحه overflow افقی ندارد. typecheck، ۶۱ تست Vitest، ۵۷ تست unittest و health/ready موفق شدند.

ممیزی مستقیم درخت GitHub نیز انجام شد: هر دو branch دارای ۲۰۵ فایل tracked، درخت کامل و بدون truncation هستند و این تعداد با checkout لوکال یکسان است. مواردی که صرفاً به‌دلیل نام حساس علامت‌گذاری شدند `web/.env.example`، migration احراز هویت و اسکریپت تست reset password هستند؛ secret واقعی در درخت دیده نشد و `.env` با `.gitignore` پوشش داده شده است.
