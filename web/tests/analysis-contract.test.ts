import fs from'fs';import path from'path';import{describe,expect,it}from'vitest';
describe('backend question contract',()=>{const source=fs.readFileSync(path.resolve('server/analysisContract.ts'),'utf8');it('returns exactly questions one through three',()=>{expect(source).toContain('current.find(q=>q.id===1)');expect(source).toContain('current.find(q=>q.id===2)');expect(source).toContain('current.find(q=>q.id===3)');});it('does not invent inflation data',()=>{expect(source).toContain('نرخ تورم مرجع دارای منبع و تاریخ در دسترس نیست');expect(source).not.toMatch(/\b\d{2}(?:\.\d+)?%/);});});
