# سند تاریخی — مرجع فعلی نیست

این گزارش مربوط به بازبینی ۲۰ مرداد ۱۴۰۵ است و اعداد، commitها و coverage آن ممکن است قدیمی باشند. برای وضعیت واقعی لوکال، GitHub، سرور و Production فقط [AUDIT_REPORT_2026-08-26.md](AUDIT_REPORT_2026-08-26.md) را ملاک قرار دهید.

# گزارش نسخه پایدار بورس‌نگار

تاریخ بازبینی: ۲۰ مرداد ۱۴۰۵

## وضعیت کد

- شاخه: `agent/data-engine-v1`
- آخرین commit پایه: `f868c12 feat: add provider fallback for fundamental analysis`
- مسیر Codal از BrsApi به‌عنوان provider پشتیبان، با cache، pagination، فیلتر گزارش‌های مالی و خطای امن فعال است.
- تحلیل برای داده‌ی ناکافی خروجی ساختگی تولید نمی‌کند و وضعیت `INSUFFICIENT_DATA` را حفظ می‌کند.
- مدل املاک و مستغلات برای ثشاهد با مبنای صریح `book_value_proxy` اضافه شده است؛ این مبنا NAV واقعی نیست.
- مسیرهای قدیمی و ناتمام تحلیل و fallbackهای بی‌استفاده از نسخه‌ی پایدار حذف شده‌اند.

## آزمون‌های نسخه

- Python unittest: ۳۲ موفق
- Vitest: ۶۱ موفق
- TypeScript typecheck: موفق
- Production build: موفق
- `git diff --check`: موفق
- بررسی متداول Secret در کد و تنظیمات نمونه: مورد حساس آشکار نشد.

## وضعیت تولید

- release نهایی وب و data service فعال و rollback-ready است.
- PM2 و سرویس FastAPI فعال‌اند.
- health و readiness با `mail=ready` تأیید شده‌اند.
- backupهای تولید و release قبلی حفظ شده‌اند.

## محدودیت‌های شناخته‌شده

- سهمیه‌ی رایگان `CODAL_Announcement` در BrsApi محدود است؛ برای مصرف بالاتر باید همان حساب فعلی ارتقا یابد.
- ساخت حساب یا کلید دوم برای دورزدن سهمیه مجاز نیست.
- فعال‌سازی ایمیل تراکنشی فقط به تنظیمات معتبر SMTP وابسته است.

## سیاست پاک‌سازی

اسکریپت‌های backup، deploy، health و rollback نگه داشته شده‌اند، چون بخشی از ایمنی عملیاتی سایت هستند. فایل‌های محیطی، dependencyها، buildها و داده‌های تولیدی وارد Git نمی‌شوند.
