# وضعیت نسخه پایدار بورس‌نگار

این شاخه نسخه‌ی پایدار و قابل انتشار فعلی است.

## باقی‌مانده‌های وابسته به سرویس بیرونی

- افزایش سهمیه‌ی `CODAL_Announcement` در BrsApi در صورت رشد مصرف سایت؛ فعلاً هیچ کلید دوم یا دورزدن سهمیه‌ای استفاده نمی‌شود.
- فعال‌سازی ارسال ایمیل تراکنشی فقط در صورت نیاز و پس از فراهم‌شدن تنظیمات SMTP معتبر.

## اصول نگهداری

- داده‌ی ساختگی یا مقدار پیش‌فرض مالی وارد تحلیل نشود.
- در نبود داده‌ی کافی، خروجی `INSUFFICIENT_DATA` و توضیح منبع نمایش داده شود.
- قبل از هر انتشار، تست Python، تست وب، typecheck، build و بررسی سلامت تولید اجرا شود.
- backupها، releaseهای rollback و اسکریپت‌های عملیاتی حذف نشوند.
